---
import BaseLayout from "../layouts/BaseLayout.astro";
import { sitePath } from "../lib/urls";
---

<BaseLayout title="页面没有找到">
  <section class="not-found shell">
    <h1>页面不存在</h1>
    <p>链接可能已失效，或章节尚未发布。</p>
    <a class="back-link" href={sitePath()}>← 返回书架</a>
  </section>
</BaseLayout>
